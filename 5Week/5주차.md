# 실습 로컬 환경 
Apple M2 Max 

# 목표 
- 5주차 : Advanced Networking - BGP, Service(LB, TrafficPolicy, ECMP), Multi-Cluster

# 실습 환경 구성 

[[5Week/실습 환경]] 

# Cilium BGP Control Plane
[[Cilium BGP Control Plane]]
[[Cilium BGP Control Plane#^e71ff2]] - Descheduler
[[Cilium BGP Control Plane#^31a55f]] - Cilium BGP로 ClusterIP를 광고해보고 통신 확인
[[Cilium BGP Control Plane#^df858c]] - Internal Traffic Policy : Local 설정 시 CusterIP로 호출 시 어떻게 동작하는지 정리
[[Cilium BGP Control Plane#^0119ef]] - Cilium 과 FRR 간 BGP 연동 시, 보안을 위해 MD5 Password 설정
[[Cilium BGP Control Plane#^e3e50a]] - L2 Announcement + Service(LB EX-IP, ExternalTrafficPolicy:Cluster) + DSR + Maglev 설정 후 인입 과정 분석


# Kind
[[Kind]]

# ClusterMesh
[[Cluster Mesh]]
[[Cluster Mesh#^7f1174]] - kind 와 같은 네트워크로 frr 컨테이너를 배포(bgp config 주입)하여, cilium 과 BGP(ECMP) + LB IPAM 로 인입 설정
[[Cluster Mesh#^ca66e8]] - 만약 west 는 요청 클라이언트만 있고, east 는 대상 서버만 존재 시, ClusterMesh 서비스 통신 최적 설정을 샘플 App대상
[[Cluster Mesh#^073695]] - Cilium ClusterMesh 환경에서 NetworkPolicies
[[Cluster Mesh#^0c9de0]] - DataStore(Kvstore) 를 설정해보고, Cilium 기본 CRD 모드와의 장단점 비교 정리
[[Cluster Mesh#^d2933e]] - Cilium ClusterMesh
[[Cluster Mesh#^da954e]] - kind k8s 클러스터를 3개를 생성 후, 3개의 클러스터 모두를 ClusterMesh로 연결

# Cilium MCS API (Beta)
[[Cilium MCS API (Beta)]]
[[Cilium MCS API (Beta)#^02832a]] - Cilium MCS API 설정 테스트 해보고, Cilium ClusterMesh 와 장단점 비교해보기